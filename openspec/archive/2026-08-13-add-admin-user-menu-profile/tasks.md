---
change_id: add-admin-user-menu-profile
status: applied
created_at: 2026-08-10 09:30:11
updated_at: 2026-08-10 10:23:28
---

# Tasks

## 1. UI Skeleton 与设计承接

- [x] 1.1 在实现前确认 `prototype/web/context.md` 与 `prototype/web/prototype.html` 的页面清单、组件层级、状态矩阵、数据依赖和 1440px 验收焦点。
- [x] 1.2 建立 `ProfileModal` UI Skeleton：用户菜单触发、Modal 容器、用户摘要文本、单头像上传区、昵称输入、错误区、底部操作、状态容器、数据依赖和可测选择器。
- [x] 1.3 确认个人资料 Modal 使用专属宽度类，不让通用 `modal-card` 与专属宽度类并存。
- [x] 1.4 确认 Modal 内只出现一个头像图标，且仅位于头像上传区；用户名和角色摘要使用文本展示。

## 2. 后端当前用户资料能力

- [x] 2.1 新增当前登录用户资料更新 API，挂在后台认证或当前用户能力域，并受后台认证依赖保护。
- [x] 2.2 新增请求/响应 schema，请求只允许 `nickname` 与 `avatar_url`，响应返回更新后的当前用户摘要。
- [x] 2.3 后端使用认证上下文中的当前用户 ID 作为更新目标，不接受请求体目标用户 ID。
- [x] 2.4 实现昵称 trim、非必填、最长 128 字符校验，清空昵称后保存为空值或等价空状态。
- [x] 2.5 校验头像 URL 为持久可访问 URL，不得保存 `blob:` URL 或临时浏览器 URL。
- [x] 2.6 确保错误响应受控，不泄露数据库错误、对象存储内部路径、堆栈或敏感配置。

## 3. 前端用户菜单与个人资料 Modal

- [x] 3.1 将用户菜单“个人资料”从占位 toast 改为打开个人资料 Modal。
- [x] 3.2 Modal 打开时展示用户名、角色摘要、单头像预览位和昵称输入项，不展示角色/状态/冻结/删除/重置密码等用户管理操作。
- [x] 3.3 头像上传区复用创建用户表单交互：单头像预览位、右侧格式提示、上传/更换/上传中按钮、隐藏 file input。
- [x] 3.4 上传头像时实现 `idle -> uploading -> done/failed` 状态机，上传中禁用重复选择和保存，失败后允许重试。
- [x] 3.5 昵称输入支持最长 128 字符，保存前 trim；清空后菜单回退展示用户名。
- [x] 3.6 点击取消、关闭 Modal 或未保存离开时丢弃头像和昵称草稿。
- [x] 3.7 保存成功后刷新当前用户摘要、本地会话缓存和用户菜单头像/昵称展示。
- [x] 3.8 保存失败时保留用户输入和头像选择状态，展示可恢复错误并允许重试。

## 4. 测试

- [x] 4.1 后端测试覆盖成功更新头像和昵称、清空昵称、昵称超长、未登录、会话失效、请求体指定目标用户、越权字段、`blob:` URL 或临时 URL 拒绝。
- [x] 4.2 后端回归当前用户 `/me`、登录摘要、后台用户管理头像上传和用户编辑相关测试。
- [x] 4.3 前端测试覆盖菜单打开 Modal、单头像预览位、上传/更换按钮文案、上传中禁用、取消丢弃草稿、保存成功刷新菜单、保存失败保留输入。
- [x] 4.4 回归用户菜单“修改密码”“界面主题”“退出登录”入口不被个人资料改动破坏。

## 5. 文档与验收

- [x] 5.1 同步 API 文档和 OpenAPI，记录当前用户资料更新接口、请求字段、响应摘要和认证错误。
- [x] 5.2 如客户端生成物受影响，重新生成并复核前端 API 客户端。
- [x] 5.3 执行 admin-modal 横切验收：computed width、低视口滚动、遮罩滚动和字段/操作区无遮挡。
- [x] 5.4 执行 media-upload 横切验收：上传状态机、失败重试、重复选择禁用、即时回显和敏感 URL 不入日志。
- [x] 5.5 在 Docker 本地 `:3000` 边界完成头像上传、读取和回显验收。
- [x] 5.6 执行 1440px 视觉验收，并记录截图或等价证据入口。
- [x] 5.7 回填 REQ-0011 `trace.md` / `acceptance.md` 中 prototype gate、视觉证据和最终一致性结果。

## 验收返修记录

### 2026-08-10 10:04:39 / opsx.modify

- 反馈：刚上传的当前用户头像可在个人资料 Modal 中预览，但保存后用户菜单栏和用户列表无法正常显示。
- 调整：前端新增头像显示 URL 规范化逻辑，渲染菜单、列表、编辑/个人资料弹窗已有头像时将持久相对 URL 补齐为 API base 下的可访问地址；创建/编辑用户上传头像时保存后端返回的持久 URL，仅将 `blob:` 对象 URL 用于即时预览。
- 文档决策：验收标准和 API/DB/权限/对象存储边界未变化；无需调整需求 AC、API 文档或 OpenSpec delta，仅补充 Change 返修记录、Trace 与 Sprint 验收证据。
- 验证：前端测试 4 files / 30 tests passed；前端构建 passed；1440px 交互截图 `/tmp/moonbox-req0011-profile-avatar-modify-1440.png` 显示保存后菜单和列表均引用持久头像 URL。

### 2026-08-10 10:11:44 / opsx.modify

- 反馈：用户截图确认问题仍存在；菜单栏和用户列表显示破图。进一步定位后确认头像读取接口要求后台认证，但普通 `<img src="/api/...">` 请求不会携带 `Authorization` header。
- 调整：新增统一鉴权头像展示组件，菜单栏和用户列表使用当前 session token `fetch` 持久头像 URL，成功后转为 `blob:` object URL 再交给 `<img>`；已有 `blob:` / `data:` 预览 URL 继续直接展示，组件卸载时释放 object URL。
- 文档决策：后端接口、API 合约、DB、对象存储和权限边界保持不变；这是前端展示链路修正，仅补充 Change 返修记录、Trace 与 Sprint 验收证据。
- 验证：前端测试 4 files / 30 tests passed；前端构建 passed；1440px 交互截图 `/tmp/moonbox-req0011-profile-auth-avatar-modify-1440.png` 显示保存后菜单栏和用户列表均为 `blob:` 图像，头像 GET 请求命中 3 次且均携带 `Bearer admin-token`。

### 2026-08-10 10:23:28 / opsx.modify

- 反馈：菜单栏和用户列表头像已显示但不一致；个人资料编辑弹窗和用户编辑弹窗仍无法正常显示已有头像。
- 调整：将头像鉴权读取提升为统一缓存链路，同一个持久头像 URL 只生成一份 `blob:` object URL，菜单栏、用户列表、个人资料弹窗和用户编辑弹窗共同复用；个人资料保存成功后同步更新 `currentSessionUser` 与 `users` 列表中的当前用户行。
- 文档决策：后端接口、API 合约、DB、对象存储和权限边界保持不变；这是前端展示缓存和状态同步返修，仅补充 Change 返修记录、Trace 与 Sprint 验收证据。
- 验证：前端测试 4 files / 31 tests passed；前端构建 passed；1440px 交互截图 `/tmp/moonbox-req0011-profile-shared-avatar-modify-1440.png` 显示菜单栏、用户列表、个人资料弹窗和用户编辑弹窗四处头像均复用同一个 `blob:`，头像 GET 鉴权命中 1 次。
