# Tasks

## 1. UI Skeleton

- [x] 1.1 基于 REQ prototype 拆解实现或调整用户管理页 UI Skeleton，覆盖页头、筛选栏、表格、分页、解冻确认弹窗、fixed toast、状态容器、数据依赖和可测选择器。
- [x] 1.2 补齐解冻确认弹窗恢复目标提示、冻结前状态展示、状态筛选和 fixed toast 的稳定布局占位。

## 2. 后端状态模型与迁移

- [x] 2.1 新增 `status_before_freeze` 或等价冻结前状态持久化能力，并保持 SQLite/MySQL 兼容。
- [x] 2.2 调整用户创建逻辑，确保新建用户主状态为待激活，临时密码仅用于首次登录激活或后续正常登录。
- [x] 2.3 调整冻结逻辑，记录待激活/正常的冻结前状态，重复冻结已冻结用户不得覆盖既有冻结前状态。
- [x] 2.4 调整解冻逻辑，根据冻结前状态恢复为待激活或正常，成功后清空冻结前状态；缺少冻结前状态时返回受控错误或明确兼容策略。
- [x] 2.5 保持已删除用户不可登录、不可首次激活、不可冻结、不可解冻或恢复为可用状态。

## 3. 后台认证与会话

- [x] 3.1 调整后台登录流程，允许待激活后台管理员使用有效临时密码首次登录并自动转为正常。
- [x] 3.2 确保前台用户、已冻结用户、已删除用户不得通过后台登录或首次激活创建后台会话。
- [x] 3.3 确保冻结、删除、重置密码或影响权限的状态变化使相关后台会话失效。
- [x] 3.4 登录失败响应使用受控文案，不泄露账号枚举、冻结前状态字段、密码哈希或内部状态细节。

## 4. 管理后台前端

- [x] 4.1 更新用户列表状态标签、冻结前状态或恢复目标展示、状态筛选和空态/分页一致性。
- [x] 4.2 更新冻结/解冻行内操作与确认弹窗，解冻时展示“恢复为待激活/正常”，且不得调用 `window.confirm`。
- [x] 4.3 更新创建用户和重置密码的一次性临时密码说明，避免固化“只有正常账号可登录”的旧文案。
- [x] 4.4 使用 fixed toast 表达成功/失败反馈，避免列表、分页、弹窗布局位移。

## 5. API、文档与契约

- [x] 5.1 同步用户 Schema、API 响应、OpenAPI 和 `docs/03-api-index.md`，体现冻结前状态或等价恢复目标。
- [x] 5.2 同步 `docs/04-database-design.md`，记录冻结前状态字段、约束和 SQLite/MySQL 兼容策略。
- [x] 5.3 同步安全、测试或认证相关文档中关于待激活首次登录和状态恢复的规则。

## 6. 测试与验收

- [x] 6.1 后端测试覆盖新建后台管理员待激活、首次登录自动转正常、前台用户拒绝后台登录、已冻结/已删除拒绝登录。
- [x] 6.2 后端测试覆盖待激活冻结后解冻仍待激活、正常冻结后解冻恢复正常、重复冻结不覆盖冻结前状态、缺少冻结前状态的受控处理。
- [x] 6.3 前端测试或浏览器验收覆盖状态筛选、状态标签、冻结前状态展示、解冻确认弹窗恢复目标提示和 fixed toast。
- [x] 6.4 在 1440px 视口完成视觉验收，记录筛选栏、表格、弹窗 computed width、低视口滚动和 fixed toast 不位移证据。
- [x] 6.5 运行 OpenSpec 严格校验、相关后端 pytest、前端 Vitest/Testing Library 和必要构建验证。

## 7. 追溯与最终一致性

- [x] 7.1 实现完成后更新 Change trace 的 skeleton、1440px 视觉验收和验证记录。
- [x] 7.2 归档前回填 REQ 最终一致性检查，确认实现、Change design、验收证据与 REQ-0007 一致。
