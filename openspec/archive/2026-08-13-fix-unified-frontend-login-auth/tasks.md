---
change_id: fix-unified-frontend-login-auth
status: applied
created_at: 2026-08-11 23:26:45
updated_at: 2026-08-11 23:38:04
---

# Tasks

## 1. 后端认证与权限

- [x] 1.1 设计统一登录接口、响应 schema 和服务端会话校验逻辑。
- [x] 1.2 支持“正常”或“待激活”用户统一登录，待激活首次登录成功后自动激活为“正常”。
- [x] 1.3 为前台接口提供统一登录态依赖，避免继续要求后台管理员角色。
- [x] 1.4 保持 `/api/v1/admin/**` 后台接口只允许后台管理员或超级管理员访问。
- [x] 1.5 确保冻结、删除、账号不存在、密码错误用户登录失败且不创建有效会话。

## 2. Web 前端

- [x] 2.1 将 `/login` 提交切换为统一登录语义，成功后默认进入前台需求中心。
- [x] 2.2 前台 session 保存服务端可校验登录态摘要和凭证，不再只是 username 镜像。
- [x] 2.3 前台用户菜单根据 `can_access_admin` 或等价后台权限字段展示“进入后台”入口。
- [x] 2.4 普通前台用户不得展示后台入口，直接访问后台路由时必须被拒绝或引导回前台/登录。
- [x] 2.5 统一登出、会话失效、修改密码成功后的前后台本地状态清理。

## 3. API、文档与生成物

- [x] 3.1 更新 OpenAPI 来源和前端 API 类型/调用契约。
- [x] 3.2 更新 `docs/03-api-index.md` 的统一登录和后台权限说明。
- [x] 3.3 更新 `docs/standards/authentication.md` 的统一登录、待激活自动激活、前后台权限边界说明。
- [x] 3.4 更新受影响的前端测试 mock 和后端测试 fixture。

## 4. 回归测试

- [x] 4.1 后端集成测试：普通前台用户正常登录成功并可访问前台接口。
- [x] 4.2 后端集成测试：待激活前台用户和后台管理员首次登录后自动激活为正常。
- [x] 4.3 后端集成测试：普通前台用户访问后台 API 返回拒绝。
- [x] 4.4 后端集成测试：冻结、删除、账号不存在、密码错误均无法登录。
- [x] 4.5 前端测试：统一登录成功后默认进入 `/requirements`。
- [x] 4.6 前端测试：后台管理员或超级管理员显示“进入后台”，普通前台用户不显示。
- [x] 4.7 前端测试：需求中心上下文请求使用统一登录态，后台相关测试不回归。
- [x] 4.8 回归登录页密码显示/隐藏、前台用户菜单个人资料、修改密码和从后台返回前台 session 状态。

## 5. 知识沉淀

- [x] 5.1 评估是否需要在 `docs/knowledge-base/incidents/` 沉淀统一登录与权限边界事故复盘；若不适用，在 trace 中说明原因。
