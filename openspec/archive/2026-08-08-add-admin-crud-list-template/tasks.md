## 1. 模板与组件边界

- [x] 1.1 梳理用户管理页中可复用的页头、筛选、表格、分页、弹窗、toast、加载、空状态和错误状态逻辑
- [x] 1.2 创建后台 CRUD 列表页模板或等价组合模式，支持页面传入业务字段、筛选项、表格列、状态标签、行级操作和主操作
- [x] 1.3 抽取或规范筛选栏、数据表格、分页、确认弹窗、表单弹窗、toast、空状态和加载状态组件边界
- [x] 1.4 确保模板与现有后台 Shell、页头、导航和全局反馈区域兼容

## 2. 用户管理页适配

- [x] 2.1 将用户管理页迁移到后台 CRUD 列表页模板或等价组合模式
- [x] 2.2 保留用户管理页业务字段、接口调用、头像上传、临时密码、超级管理员保护和状态治理规则
- [x] 2.3 回归用户管理页列表、筛选、分页、创建、编辑、冻结、解冻、逻辑删除、重置密码、头像回显和临时密码弹窗
- [x] 2.4 移除或收敛用户管理页中可复用的大段页面级 JSX/CSS 重复实现

## 3. 横切 UI 验收

- [x] 3.1 验证分页 DOM 与用户管理页基准一致：总数左侧，翻页、页码、“每页显示”和条数下拉右侧
- [x] 3.2 验证成功和失败反馈使用 fixed toast，且不挤压列表、分页、弹窗或抽屉内容
- [x] 3.3 验证状态变更使用设计系统确认弹窗，且不调用 `window.confirm`
- [x] 3.4 验证表格行内操作列在横向滚动或列较多时保持易访问
- [x] 3.5 验证新增、编辑或确认弹窗 computed width 符合设计预期
- [x] 3.6 验证低视口下弹窗 body 可滚动，底部主操作和取消操作可访问

## 4. 测试与文档

- [x] 4.1 补齐前端测试，覆盖模板渲染、筛选、分页、空状态、加载状态、错误状态、状态操作和 toast 反馈
- [x] 4.2 补齐用户管理页模板化适配回归测试
- [x] 4.3 运行 `pnpm --dir src/web test`
- [x] 4.4 运行 `pnpm --dir src/web build`
- [x] 4.5 运行 `openspec validate add-admin-crud-list-template --strict`
- [x] 4.6 归档前同步相关长期文档，或明确 API、数据库、部署、安全和客户端生成物不适用
