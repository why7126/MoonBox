## 1. Backend And Data Model

- [x] 1.1 设计并实现用户角色、状态、逻辑删除、空间数聚合和超级管理员保护所需的数据字段或查询路径
- [x] 1.2 实现管理后台用户列表、搜索、角色筛选、状态筛选和分页 API
- [x] 1.3 实现创建用户、编辑用户、冻结、解冻、逻辑删除和重置密码 API
- [x] 1.4 实现冻结后 10 秒内目标用户有效会话失效机制
- [x] 1.5 实现用户管理写操作审计，记录操作者、对象、动作、前后值、原因、结果、时间和请求 ID

## 2. Avatar Upload And Storage

- [x] 2.1 实现或复用头像上传接口，支持上传状态与错误反馈
- [x] 2.2 确保头像上传成功后返回可浏览器访问的头像 URL 或对象引用
- [x] 2.3 验证 Docker 本地 `:3000` 环境下头像上传、读取和回显链路

## 3. Web Admin UI

- [x] 3.1 新增管理后台 `SYSTEM` 分组下的用户管理入口和页面
- [x] 3.2 实现用户列表、筛选栏、分页、固定操作列、状态标签和超级管理员保护行
- [x] 3.3 实现新增/编辑用户弹窗，用户名创建后不可修改，角色仅允许“后台管理员”和“前台用户”
- [x] 3.4 实现冻结、解冻、删除和重置密码的设计系统确认弹窗、原因填写和 fixed toast
- [x] 3.5 实现头像上传状态机 `idle -> uploading -> done/failed` 和同会话即时回显
- [x] 3.6 按 `prototype/web/prototype.png` 验收 1440px 信息密度，并验证 1280px 及以上不破版

## 4. Tests And Validation

- [x] 4.1 补充后端 pytest，覆盖用户名校验、角色范围、状态变更、超级管理员保护、冻结会话失效和审计
- [x] 4.2 补充 API 集成测试，覆盖列表筛选分页、创建编辑、冻结解冻删除、重置密码和头像上传
- [x] 4.3 补充前端 Vitest/Testing Library 测试，覆盖列表筛选、弹窗校验、状态操作确认、toast 和头像回显
- [x] 4.4 补充 UI/E2E 或 Playwright 验证，覆盖 fixed toast 无 layout shift、无 `window.confirm`、computed width、低视口滚动和 Docker `:3000` 上传回显

## 5. Documentation And Sync

- [x] 5.1 同步 API 文档、数据库设计和对象存储说明
- [x] 5.2 确认 `docs/knowledge-base/best-practices/` 三份治理项在实现设计和验收中被引用
- [x] 5.3 运行 OpenSpec 校验、相关测试和 Workflow Sync

## 验收返修记录

| 时间 | 反馈 | 调整 | 验证 |
|---|---|---|---|
| 2026-08-08 10:40:21 | 用户管理页面整体效果与 REQ-0004 `prototype.html` v1.0.5 差异较大，当前实现偏简化后台壳，未体现侧边栏收起、底部用户菜单、主题入口和 1440px 信息密度。 | 按 `prototype.html` v1.0.5 对齐 React 后台 Shell：补齐侧边栏收起/展开、底部用户菜单、主题 switch、接口文档导航项、版本标识、原型式导航 icon/label、用户管理页头、筛选栏、表格、分页和 sticky 操作列的密度样式；补充前端测试锁定原型壳关键交互。 | `pnpm --dir src/web test` 12 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。当前环境缺少可用 Playwright 包，未能生成 1440px 截图，已用 DOM/测试锁定原型壳关键交互。 |
| 2026-08-08 10:46:53 | 管理后台左上角图标仍为文字占位，需改成产品图标。 | 使用现有 MoonBox 产品图标 `/brand/moonbox/moonbox-app-icon-256.png` 替换后台 Shell 左上角文字标记，并补充前端断言锁定图标资源。 | `pnpm --dir src/web test` 12 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 10:54:11 | 侧边栏、用户菜单栏菜单图标仍为文字/符号占位，需更换成当前菜单的图标。 | 使用既有 `lucide-react` 图标库，为首页、空间管理、用户管理、系统设置、日志审计、接口文档、个人资料、密码修改、主题、退出登录分别替换语义图标，并补充前端测试锁定导航和用户菜单图标渲染数量。 | `pnpm --dir src/web test` 12 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 11:11:28 | 当前菜单图标仍显得偏重、偏杂，需参考 ProjectTilesFST 后台侧边栏图标风格继续收敛；空间管理表达为组织空间。 | 将侧边栏导航图标收敛为 `LayoutDashboard`、`Network`、`Users`、`Settings`、`ListTree`、`BookOpen`，统一为 16px / stroke 1.5 / 默认 0.72 透明度 / active 与 hover 提亮；用户菜单图标统一为 14px / stroke 1.5；补充不同菜单 SVG 内容差异测试。 | `pnpm --dir src/web test` 12 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 11:18:31 | 用户菜单栏主题样式异常，主题开关被用户菜单按钮通用样式误伤；主题入口需保留在用户菜单内。 | 将用户菜单按钮选择器收窄为一级按钮，避免嵌套的 `admin-theme-switch` 继承整行菜单按钮样式；为主题行补 hover 反馈，为主题开关明确 34x18 尺寸、圆角、对齐、hover/focus 状态和圆形滑块；补充前端测试锁定主题行结构。 | `pnpm --dir src/web test` 12 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 11:26:53 | 用户菜单栏主题切换按钮交互需要优化，当前只有右侧小开关可点，整行 hover 但点击文字/图标无效。 | 将主题行改为整行 `button role="switch"`，右侧开关降级为视觉状态 `span`，消除嵌套按钮；点击整行切换主题，使用切换后的状态生成 toast，并补充 `aria-checked` 与 toast 测试。 | `pnpm --dir src/web test` 12 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 12:42:59 | 切换亮主题时只有局部调整，侧边栏、用户菜单、表格、筛选栏、分页、弹窗等仍保留暗色硬编码。 | 为后台 Shell 引入主题语义 CSS 变量，暗色作为默认值，`.admin-shell.light` 重写亮主题变量；将用户管理页主要后台 UI 的背景、文字、边框、hover、overlay、toast 和控件颜色改为变量驱动；补充主题切换测试锁定 light class 与 `data-theme`。 | `pnpm --dir src/web test` 12 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 12:54:07 | 右侧内容的新增用户、用户列表、分页、用户编辑弹窗样式与 `prototype.html` v1.0.5 差异较大；用户菜单栏主题文案需改成“界面主题”。 | 补齐筛选栏自动查询提示；将用户列表列顺序调整为用户 / 所属 Workspace / 角色 / 状态 / 最近登录时间 / 创建时间 / 操作；操作列改为紧凑 `admin-operation-set`；分页改为原型式 total/actions/page-btn/page-size 结构；用户新增/编辑弹窗改为 form-row/form-hint/avatar-picker/drawer-actions 结构与 48px 头像选择器；用户菜单主题文案改为“界面主题”；补充前端结构与交互测试。 | `pnpm --dir src/web test` 13 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 13:00:06 | 文案调整：页头副文案由“统一管理平台用户、角色与账号生命周期 · admin”改为“统一管理平台用户、角色生命周期”，并删除“输入或筛选后自动查询”。 | 移除页头副文案中的账号生命周期和当前用户名后缀；删除筛选栏自动查询提示；更新前端测试断言新文案存在且旧提示不存在。 | `pnpm --dir src/web test` 13 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 14:33:47 | 新增用户按钮与 `prototype.html` v1.0.5 的 `.btn.primary` 样式不一致。 | 新增 `admin-btn` 基础按钮样式，固定 36px 高度、0 15px padding、2px 圆角和 inline-flex 居中；页头新增用户按钮使用 `admin-btn admin-primary`；`admin-primary` 字重调整为 600；补充前端测试锁定 class。 | `pnpm --dir src/web test` 13 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 14:40:41 | 新增用户按钮颜色仍与 `prototype.html` v1.0.5 不一致，需引入后台主题变量并让亮主题使用 `#B8863E`。 | 为后台 Shell 新增 `admin-gold`、`admin-gold-strong` 和 `admin-primary-text` 变量；light 主题覆盖金色为 `#B8863E`；将 `admin-primary` 及相关后台金色 UI token 改为变量驱动；补充测试锁定按钮色值不再硬编码。 | `pnpm --dir src/web test` 14 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 14:47:52 | 新增用户按钮颜色仍未变化，原因是 `.admin-content button` 的选择器优先级高于单独的 `.admin-primary`，覆盖了背景和文字颜色。 | 将 primary 覆盖选择器提升为 `.admin-content .admin-primary`、`.admin-user-modal .admin-primary` 和 `.admin-confirm-modal .admin-primary`，继续使用 `admin-gold` 与 `admin-primary-text` 变量；补充测试锁定高优先级覆盖选择器。 | `pnpm --dir src/web test` 14 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 14:56:02 | 用户列表整体与原型仍差异较大，表现为字体/权重/透明度偏离、列宽松散、日期裁切、超级管理员行列顺序需按最终表头校正，分页总数需按原型显示 28。 | 为用户表格新增 `colgroup` 列宽锚点，正文收敛到 13px、54px 行高和轻量字重，用户名/昵称/状态/操作文案显式设置可读颜色与密度；日期列不换行；默认分页总数按原型显示 28；补充表格列顺序、超级管理员行、分页总数和 CSS 密度测试。 | `pnpm --dir src/web test` 15 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 15:07:02 | 用户列表细节继续验收：头像字体偏大；表头字体比例需优化；“所属 Workspace”应改为“空间数”；系统内置标签应与用户名同行；字段间隔应更紧凑但不换行；“共 28 个用户”不是当前真实数据。 | 缩小头像字体为 12px；表头调整为 12px/600；表头文案改为“空间数”；新增 `admin-user-name-line` 让用户名和系统内置标签同行；列宽收紧到 1230px 总体最小宽度并保持日期/操作不换行；分页总数恢复 `filteredUsers.length`；同步表格结构与密度测试。 | `pnpm --dir src/web test` 15 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 15:14:33 | 用户新增/编辑弹窗与原型差异较大；分页每页显示需支持 10 条、20 条、50 条、100 条。 | 收敛用户表单弹窗为原型式 560px、22px 内边距和小字号层级；修复必填星号与 label 同行；输入框/选择框 40px，头像 picker 48px，上传按钮和底部按钮 36px；hint/error 使用低层级文本；分页选项新增 50 条和 100 条；补充弹窗结构、样式和分页选项测试。 | `pnpm --dir src/web test` 16 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 15:26:03 | 用户表单继续验收：删除“用户名需以字母开头，且为 4-32 位字母或数字。”文案；头像按钮根据状态显示“上传/更换”；按钮字号与“支持 JPG、PNG，建议 1:1”一致并分两行；上传不能停留在假实现。 | 移除用户名错误提示渲染，仅保留保存按钮禁用约束；上传按钮默认“上传”，已有头像或上传成功后显示“更换”，上传中显示“上传中”；按钮与说明改为两行 `admin-avatar-copy`，字号一致；新增隐藏 file input，选择文件后通过 FormData 调用 `/api/v1/admin/users/avatar`，使用 Bearer token，并保留上传失败状态；同步真实上传测试。 | `pnpm --dir src/web test` 16 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 15:39:09 | 用户头像上传按钮与“支持 JPG、PNG，建议 1:1”需互换位置；上传按钮宽度应根据文字更紧凑。 | 将头像说明文案置于上传/更换按钮上方；`admin-avatar-copy` 左对齐；上传按钮改为 `width: fit-content` 与 0 10px padding，随“上传/更换”文案自适应宽度；补充 DOM 顺序与紧凑样式测试。 | `pnpm --dir src/web test` 16 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 15:49:08 | 上传按钮高度需要再小一点。 | 将头像上传/更换按钮高度与最小高度从 36px 调整为 28px，保持 `width: fit-content` 与紧凑横向 padding，并同步样式测试断言。 | `pnpm --dir src/web test` 16 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 15:55:36 | 头像上传失败，且当前失败文案无法区分后端错误；上传成功后的头像 URL 仍是需 Bearer 的接口，直接作为 `img src` 会导致回显失败。 | 上传失败时读取后端 `detail/message` 并展示具体错误；上传成功后使用后端返回 URL 继续带 Bearer token 授权 fetch 读取头像资源 blob，转为 `URL.createObjectURL` 给当前表单和保存后的列表回显；补充成功上传后授权读取回显测试和失败详情测试。 | `pnpm --dir src/web test` 17 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 16:02:18 | 用户表单校验异常时不同位置 tips 不统一，按钮禁用成为部分场景唯一反馈。 | 新增统一 `admin-form-error` 字段错误样式；用户名非法、头像上传失败、操作原因不足 4 字均在字段下方展示错误；字段 hint 常驻，不再被错误文案替换；确认弹窗操作原因补充常驻 hint 与字段级错误；同步测试覆盖三类错误提示。 | `pnpm --dir src/web test` 17 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 16:13:03 | 管理后台整体字体观感不统一，侧边栏、用户菜单、页头、筛选栏、表格、分页、弹窗、toast 和按钮存在分散硬编码字号/字重/行高。 | 为 `.admin-shell` 新增 `admin-font-*`、`admin-text-*`、`admin-leading-*` 和 `admin-weight-*` 语义 token；后台正文统一使用 `Noto Sans SC`，标题保留 `Noto Serif SC`，英文品牌/点缀保留 `EB Garamond`；收敛菜单、页头、表格、分页、弹窗、toast 和按钮字体来源；补充后台字体一致性测试。 | `pnpm --dir src/web test` 18 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`git diff --check` passed。 |
| 2026-08-08 16:23:14 | 头像上传没有进入 MinIO，当前实现写入后端本地 `data/media/avatars`，与 `self-storage-sqlite` 自建 MinIO 验收预期不一致。 | 新增 MinIO SDK 依赖和 `app.core.object_storage` 封装；头像上传改为写入单 Bucket 的 `images/avatars/{uuid}.{ext}`，读取接口通过后台授权从 MinIO 拉取并回显；同步 Docker/backend env、API 文档、对象存储策略、上传标准、OpenSpec delta、REQ 验收和 Sprint 范围说明；集成测试使用 fake storage 锁定 object key 与授权读取。 | `uv run pytest tests/integration/api/test_admin_users.py` 9 passed；`pnpm --dir src/web test` 18 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`deploy/scripts/validate-env.py self-storage-sqlite` passed；`docker compose config` passed；`compileall` passed；`git diff --check` passed。 |
| 2026-08-08 16:46:09 | `self-storage-sqlite` 启动后头像上传仍报 `NetworkError`，原因是 `.env` 中 `HOST_PORT_BACKEND=8001` 但 `VITE_API_BASE_URL=http://localhost:8000`，浏览器请求未打到后端。 | `scripts/docker-up.sh` 启动前读取环境变量和 `.env` 校验浏览器 API 端口；未显式设置 `VITE_API_BASE_URL` 时自动导出 `http://localhost:${HOST_PORT_BACKEND}`；显式 localhost 端口不一致时直接失败并给出修正提示；启动输出使用解析后的端口；同步部署文档和脚本单测。 | `bash -n scripts/docker-up.sh` passed；`uv run pytest tests/unit/test_docker_up_script.py tests/integration/api/test_admin_users.py` 11 passed；`pnpm --dir src/web test` 18 passed；`pnpm --dir src/web build` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`deploy/scripts/validate-env.py self-storage-sqlite` passed；`docker compose config` passed；`git diff --check` passed。 |
| 2026-08-08 17:08:42 | 本地环境变量默认值分散且与项目端口、时区和对象存储约定不一致：宿主机端口仍使用通用 8000/5173/9000，容器内端口仍可配置，默认 bucket 为 `moonbox-assets`，自建 MinIO 使用命名卷或 `data/minio`。 | 移除可配置容器内端口变量；宿主机端口统一为 `18101-18199` 默认段；默认 `TZ=Asia/Shanghai`、`DATABASE_TIMEZONE=+08:00`；默认 `MINIO_BUCKET=moonbox`；自建 MinIO 数据目录统一为项目根 `data/s3`；同步 root/local Compose、`.env.example`、真实本地 `.env` 非密钥项、deploy/local env、后端 env 示例、部署/端口/对象存储文档，并补充本地端口范围和时区校验测试。 | `uv run pytest tests/unit/test_docker_up_script.py tests/unit/test_validate_env.py tests/integration/api/test_admin_users.py tests/unit/test_database_config.py` 18 passed；`pnpm --dir src/web test` 18 passed；`pnpm --dir src/web build` passed；`py_compile validate-env` passed；`bash -n scripts/docker-up.sh` passed；全部 deploy/local env example 校验 passed；`docker compose config` passed；`openspec validate add-admin-user-management --strict` passed；`validate-openspec-language.py` passed；`validate-env-ignore-policy.py` passed；`validate-directory-structure.py` passed；`git diff --check` passed。 |
| 2026-08-08 18:27:58 | 用户列表数据不是真实数据，页面仍使用前端 `initialUsers` 演示数组，导致 `chenmo/linyu/zhaoqi` 等样例用户与数据库事实源不一致。 | 移除前端运行时 mock 用户数组；用户列表加载、搜索、角色/状态筛选和分页改为授权调用 `/api/v1/admin/users`；新增/编辑/冻结/解冻/逻辑删除/重置密码改为调用对应后台 API 并刷新真实列表；分页总数使用后端 `total`；头像上传继续保存后端受控 URL，预览使用授权 blob URL；补充真实 API fetch、筛选分页、错误 toast 和后端契约测试。 | `pnpm --dir src/web test` 18 passed；`pnpm --dir src/web build` passed；`uv run pytest tests/integration/api/test_admin_users.py` 9 passed。 |
| 2026-08-08 18:38:55 | 用户创建后没有登录密码；重置密码虽然返回临时密码，但未写入 `password_hash`，导致返回的新密码也无法登录。 | 创建用户时生成一次性临时密码并写入 `admin_users.password_hash`，创建接口返回 `user + temporary_password + message`；重置密码时将新临时密码 hash 写回 `password_hash` 并撤销目标用户会话；前端创建成功 toast 仅展示一次临时密码；补充创建后临时密码可登录、重置后旧密码失效新密码可登录、前端一次性展示测试。 | `pnpm --dir src/web test` 18 passed；`pnpm --dir src/web build` passed；`uv run pytest tests/integration/api/test_admin_users.py` 10 passed。 |
| 2026-08-08 18:45:56 | 创建用户和重置密码后的临时密码仅用 toast 展示，停留时间短且不利于复制。 | 新增 `TemporaryPasswordModal`，创建用户和重置密码成功后改为弹窗展示用户名、一次性临时密码、安全提示、复制按钮和“我已复制并关闭”；复制成功显示“已复制”并以 toast 提示，不再在 toast 中承载密码；关闭弹窗后清空前端临时密码状态且不持久化。 | `pnpm --dir src/web test` 20 passed；`pnpm --dir src/web build` passed。 |
| 2026-08-08 18:52:47 | 临时密码弹窗底部按钮“我已复制并关闭”文案别扭，像替用户确认复制事实。 | 将底部按钮文案改为“关闭”，复制状态继续由复制按钮“复制/已复制”表达；安全提示改为“关闭后将无法再次查看该密码，请确认已妥善保存。”并保留不持久化说明；同步前端测试。 | `pnpm --dir src/web test` 20 passed；`pnpm --dir src/web build` passed。 |
| 2026-08-08 19:04:31 | 弹窗右下角按钮过窄，确认弹窗和临时密码弹窗 footer 按钮缺少稳定横向尺寸。 | 将 `.admin-user-modal footer button`、`.admin-confirm-modal footer button` 和 `.admin-password-modal footer button` 统一设置为 36px 高度、72px 最小宽度、0 15px padding 和 2px 圆角，上传按钮紧凑样式继续限定在 `.admin-avatar-picker button`；补充样式测试。 | `pnpm --dir src/web test` 20 passed；`pnpm --dir src/web build` passed。 |
| 2026-08-08 19:15:43 | 用户列表不应默认显示已删除用户，“全部状态”也不应混入逻辑删除数据，仅显式选择“已删除”时查看。 | 后端列表仓储在未传状态筛选时默认排除 `status=已删除`，显式 `status=已删除` 时只返回逻辑删除用户且 `total` 同规则计算；前端状态筛选保留“已删除”入口，已删除用户操作列显示“不可操作”；补充后端列表筛选和前端状态筛选测试。 | `pnpm --dir src/web test` 21 passed；`uv run pytest tests/integration/api/test_admin_users.py` 11 passed。 |
| 2026-08-08 19:20:56 | 状态筛选项不应提供“已删除”。 | 前端状态筛选下拉移除“已删除”，类型收敛为“全部状态/待激活/正常/已冻结”；页面默认继续隐藏逻辑删除用户；更新前端测试断言状态选项不包含“已删除”，并同步 OpenSpec 规格和设计说明。 | `pnpm --dir src/web test` 21 passed；`pnpm --dir src/web build` passed。 |
