## 1. UI Contract 与 Skeleton

- [x] 1.1 基于 prototype.html、prototype.png、prototype-context.md、interaction.md、acceptance.md 和 `rules/ui-design.md` 完成空间管理 UI Skeleton 首轮实现，不接入业务细节前先固定页面壳、导航入口、页签、列表、详情、弹窗层和 fixed toast 容器
- [x] 1.2 在 1440px 桌面视口验收 Skeleton，记录空间列表、申请审批、回收站、空间详情和至少一个弹窗的首轮视觉证据
- [x] 1.3 在 Change trace 中声明 Mock/API 边界、prototype 冲突处理、UI Skeleton 状态和待接入真实 API 区域

## 2. 后端数据模型与迁移

- [x] 2.1 新增空间、空间产品绑定、空间成员关系、空间申请、空间配额/用量、空间审计和生命周期状态相关模型与迁移
- [x] 2.2 实现空间唯一标识、名称冲突、有效期类型、受保护空间、状态流转和一空间一产品绑定约束
- [x] 2.3 补充数据库设计文档或变更说明，记录空间、产品绑定、配额、审计和回收期字段

## 3. 后端 API 与服务

- [x] 3.1 实现空间列表、创建、详情、基础信息编辑、冻结、恢复、续期、负责人移交、配额调整、删除、永久删除 API
- [x] 3.2 实现空间申请查询、审批通过、拒绝和撤回/重复审批保护逻辑
- [x] 3.3 实现运行中 Agent/任务/未完成迭代删除阻塞检查接口或服务层检查
- [x] 3.4 实现通知事件和不可变审计记录写入，确保不记录 token、会话 ID 明文或敏感凭证
- [x] 3.5 实现服务端权限、状态、幂等键、原因填写和 `allowedActions` 校验

## 4. Web 管理后台实现

- [x] 4.1 新增后台空间管理导航入口、空间列表页、申请审批页、回收站页和空间详情页
- [x] 4.2 接入真实空间 API，完成搜索、筛选、分页、状态化行内操作、加载态、错误态、空态和权限态
- [x] 4.3 实现创建、编辑、审批、冻结、恢复、配额调整、续期、负责人移交、删除和永久删除弹窗
- [x] 4.4 实现空间详情概览、成员、产品、配额与用量、操作记录分区，并保证基础信息、配额与用量、最近操作为独立模块
- [x] 4.5 实现有效期 `fixed_date` / `long_term` 组件复用和列表/详情“长期有效”展示

## 5. 横切 UI 与原型验收

- [x] 5.1 验收空间列表分页 DOM 与用户管理基准一致，fixed toast 不引发布局位移，筛选变化后列表/分页/空态一致
- [x] 5.2 验收高风险操作均使用设计系统确认弹窗，且代码不调用 `window.confirm`
- [x] 5.3 验收创建/编辑/审批/冻结/恢复/配额/删除/永久删除弹窗 computed width、低视口滚动、遮罩滚动和底部操作可访问
- [x] 5.4 在 1440px 桌面视口记录空间列表、申请审批、回收站、详情概览、详情分区、用户菜单主题切换、toast 和关键弹窗视觉证据
- [x] 5.5 在 Change trace 中回填 computed style 证据、关键交互证据、Mock/API 边界和 REQ 最终一致性检查

## 6. 测试与文档

- [x] 6.1 补充后端集成测试，覆盖空间创建与产品绑定、列表筛选分页、审批、冻结恢复、配额、删除回收、永久删除、权限和审计
- [x] 6.2 补充前端测试，覆盖空间列表、筛选分页、详情、弹窗、高风险确认、toast、权限态和长期有效交互
- [x] 6.3 同步 API 文档、OpenAPI/Orval、数据库文档、部署/环境说明和相关长期文档
- [x] 6.4 运行后端测试、前端测试、构建、OpenSpec 严格校验、中文优先校验和目录结构校验
- [x] 6.5 更新 REQ acceptance、trace 和 Sprint 验收报告中的实现证据入口

## 验收返修记录

- [x] 2026-08-12 返修 1：根据验收反馈，将“空间列表 / 申请审批 / 回收站”主页签从右侧内容区顶部下移到页面标题区下方、筛选栏上方，并移除主页签中的“详情”入口；详情保持为点击空间后进入的独立详情视图。
- [x] 2026-08-12 返修 1 验证：更新前端测试断言标题先于主页签、详情页无主页签；重新生成 1440px 空间列表、申请审批、回收站、详情和冻结弹窗截图；`computed-style.json` 重新记录弹窗样式。
- [x] 2026-08-12 返修 2：根据验收反馈，以用户管理页为准抽取共享后台侧边栏，空间管理页复用同款折叠、用户菜单、个人资料、修改密码、返回前台、主题切换和退出登录；空间管理 active，用户管理可导航。
- [x] 2026-08-12 返修 2 验证：补充空间页侧边栏一致性测试；重跑用户管理页回归；重新生成 1440px 空间管理视觉证据。
- [x] 2026-08-12 返修 3：根据验收反馈，创建/移交负责人不再输入 ID，改为复用用户管理列表的状态正常用户下拉；编辑弹窗移除负责人字段；创建表单补齐必填标记、输入要求、长度限制、产品信息自动派生、长期有效隐藏到期时间和固定日期时间控件。
- [x] 2026-08-12 返修 3 验证：补充前端测试覆盖负责人候选 API、创建 payload 产品派生、移交负责人下拉和编辑不含负责人字段；空间页 6 个用例通过。
- [x] 2026-08-12 返修 4：根据验收反馈，空间新增/编辑弹窗标题移除图标，必填 `*` 改为红色，创建弹窗字段顺序调整为五行结构，编辑弹窗不包含负责人和配额字段，负责人占位符改为“请选择”，下拉项不显示角色，并更新空间名称/编码帮助文案。
- [x] 2026-08-12 返修 4 验证：补充前端测试覆盖标题无图标、必填星号结构、字段顺序、描述整行、负责人下拉占位符与选项文案；刷新 1440px 新增空间弹窗视觉证据和 computed style。
- [x] 2026-08-12 返修 5：根据验收反馈，微调空间名称与空间编码帮助文案，明确“空间名称即产品名称”和空间编码“系统唯一、3-48 位、创建后不可修改”。
- [x] 2026-08-12 返修 5 验证：更新前端测试文案断言；空间页单测和前端构建通过。
- [x] 2026-08-12 返修 6：根据验收反馈，抽取统一 `AdminSelect`，替换用户管理、空间管理和后台分页中的原生下拉框，保持既有筛选、负责人选择、有效期选择和分页条数行为不变。
- [x] 2026-08-12 返修 6 验证：补充键盘选择、点击外部关闭和列表位于触发器下方的测试；刷新 1440px 新增空间弹窗下拉展开截图和 computed style 坐标证据。
- [x] 2026-08-13 返修 7：根据验收反馈，将空间列表对齐原型效果：补回 4 张统计卡，表格列调整为空间、负责人、成员、产品、AI 用量、有效期、状态、创建来源、操作；AI 用量使用进度条表达预警/超限；行内操作收敛为查看、编辑、冻结/恢复、更多；主页签样式改为原型下划线 Tab。
- [x] 2026-08-13 返修 7 验证：补充空间列表统计卡、九列表头、AI 用量进度条、创建来源和更多入口测试；刷新 1440px 空间列表截图与 computed style 原型对齐证据。
- [x] 2026-08-13 返修 8：根据验收反馈，空间列空间名称改为无边框文字链接；为空间表格补充 colgroup 列宽并调整最小宽度，确保 1440px 下操作列可见。
- [x] 2026-08-13 返修 8 验证：补充空间名称链接、colgroup 和操作入口测试；刷新 1440px 空间列表截图与 computed style，记录空间名称 `border-width: 0px`、背景透明、操作列可见。
- [x] 2026-08-13 返修 9：根据验收反馈，以用户管理列表为后台列表视觉基准，空间列表操作列复用无边框金色文字链接样式，状态展示改为圆点加文本风格，保留字段结构、统计卡、AI 用量进度条和下划线 Tab。
- [x] 2026-08-13 返修 9 验证：补充空间操作区复用 `admin-operation-set` 与状态样式测试；刷新 1440px 空间列表截图与 computed style，记录操作按钮 `border-width: 0px`、状态 `border-width: 0px` 和状态圆点。
- [x] 2026-08-13 返修 10：根据验收反馈，空间列表字段与密度继续收敛：表头调整为空间名称/编码、成员数、产品数；新增更新时间列；创建来源改为轻量标签；除首列外表头和内容不换行；冻结/恢复移除图标；修复更多菜单被表格容器裁剪。
- [x] 2026-08-13 返修 10 验证：补充字段名、更新时间、来源标签、冻结无图标测试；刷新 1440px 空间列表与更多菜单截图，computed style 记录 nowrap、来源标签和更多菜单可见。
- [x] 2026-08-13 返修 11：根据验收反馈，参照用户管理列表恢复空间列表更舒展的列宽和单元格间距，操作列宽度对齐用户管理基准；AI 用量改为数值与百分比文本，不再展示进度条；状态列改为不同语义图标/颜色加文本；更多菜单改为页面级 fixed 浮层，避免被表格 overflow 或 sticky 操作列裁剪，并支持点击外部和 Escape 关闭。
- [x] 2026-08-13 返修 11 验证：补充空间列表 AI 用量数值、状态图标、portal 更多菜单、点击外部关闭和 Escape 关闭测试；刷新 1440px 空间列表与更多菜单截图，computed style 记录 AI 用量无进度条、操作列 `220px`、状态图标和 body 级 fixed 更多菜单。
- [x] 2026-08-13 返修 12：根据验收反馈，AI 用量恢复为与原型一致的两行展示：上行进度条、下行百分比与状态，并按正常/预警/超限使用正确颜色；更多菜单保留 portal/fixed 浮层，并增加分页/视口底部边界判断，靠近分页或视口底部时向上展开，避免遮挡分页。
- [x] 2026-08-13 返修 12 验证：补充 AI 用量进度条与预警文案测试、更多菜单向上展开测试；刷新 1440px 空间列表与最后一行更多菜单截图，computed style 记录 `hasUsageProgressBar=true`、`moreMenuPlacement=top`、`moreMenuDoesNotCoverPagination=true`。
- [x] 2026-08-13 返修 13：根据验收反馈，继续微调 AI 用量与更多菜单视觉细节：AI 用量第二行仅显示百分比，不显示状态文字；提高进度条轨道对比度并调整宽度、条高和间距；更多菜单缩小宽度、字体、行高和 padding，面板保持完全不透明，普通项使用高对比金色、危险项红色。
- [x] 2026-08-13 返修 13 验证：补充 AI 用量第二行仅百分比测试；刷新 1440px 空间列表与更多菜单截图，computed style 记录 `usageText=86%`、`usageBarWidth=104px`、`moreMenuWidth=108px`、`moreMenuButtonFontSize=12px`、`moreMenuButtonColor=rgb(203, 163, 92)`。
- [x] 2026-08-13 返修 14：根据验收反馈，更多菜单定位锚定当前行“更多”按钮，优先在按钮下方并与右边缘对齐，空间不足时向上展开且仍保持右边缘对齐；补充普通项和危险项 hover/focus-visible 高亮状态。
- [x] 2026-08-13 返修 14 验证：补充更多菜单按钮锚点定位、向上展开和用户管理回归测试；刷新 1440px 最后一行更多菜单截图与 computed style，记录 `moreMenuRightAlignedToTrigger=true`、`moreMenuButtonHoverBackground=rgba(203, 163, 92, 0.16)`、`moreMenuButtonColor=rgb(228, 190, 118)`。
- [x] 2026-08-13 返修 15：根据验收反馈，更多菜单改为优先贴近当前行“更多”按钮所在操作列并从按钮左边缘展开，底部空间不足时向上展开但不横向漂移；操作原因参照 BUG-0010 补齐红色必填标记、输入提示、空值/不足 4 字确认后的可见错误、`aria-invalid` 和无效原因不发起请求。
- [x] 2026-08-13 返修 15 验证：补充更多菜单贴近按钮定位、操作原因空值/短值阻断和有效原因提交测试；刷新 1440px 更多菜单与冻结弹窗错误态截图，computed style 记录 `moreMenuLeftAlignedToTrigger=true`、`moreMenuHorizontalGapFromTrigger=0`、`reasonAriaInvalid=true`、`reasonErrorText=操作原因至少需要 4 个字。`。
- [x] 2026-08-13 返修 16：根据验收反馈，空间列表更多菜单固定显示在“更多”按钮下方，面板与按钮左对齐并保留 6px 间隙，避免重叠；提高菜单面板背景、边框和阴影对比度，使其与页面背景明确区分。
- [x] 2026-08-13 返修 16 验证：补充更多菜单下方展开和 6px 间隙测试；刷新 1440px 更多菜单截图与 computed style，记录 `moreMenuPlacement=bottom`、`moreMenuLeftAlignedToTrigger=true`、`moreMenuVerticalGapFromTrigger=6`、`moreMenuBackgroundColor=rgb(31, 34, 61)`。
- [x] 2026-08-13 返修 17：根据验收反馈，更多菜单 portal 浮层改为自身携带 `dark/light` 主题 class，背景、边框、hover 和危险项颜色分别适配暗色/浅色主题；菜单项强制 flex 左对齐和文本左对齐。
- [x] 2026-08-13 返修 17 验证：刷新 1440px 双主题 computed style，记录暗色 `moreMenuClassName=admin-space-more-menu dark`、`moreMenuButtonJustifyContent=flex-start`、浅色 `className=admin-space-more-menu light`、`backgroundColor=rgb(255, 252, 244)`、`borderColor=rgba(142, 105, 45, 0.36)`、`buttonJustifyContent=flex-start`。
- [x] 2026-08-13 返修 18：根据验收反馈，空间详情页按 REQ-0017 原型补齐详情头、面包屑、负责人摘要/变更入口、状态化操作、返回空间列表入口和概览/成员/产品/配额与用量/操作记录五个详情分区 Tab；概览重排为基础信息、配额与用量、最近操作三张同级独立卡片。
- [x] 2026-08-13 返修 18 验证：补充详情页原型结构和 5 个详情分区切换测试；刷新 `space-detail-1440.png` 与 `computed-style.json`，记录 `hasDetailHead=true`、`detailTabs=[概览,成员,产品,配额与用量,操作记录]`、`overviewGridTemplateAreas=\"base recent\" \"quota recent\"`、四个非概览分区均可见。
- [x] 2026-08-13 返修 19：根据验收对比图继续收敛空间详情视觉：面包屑改为无边框纯文本路径；详情头按原型重排为空间名称与状态同行、编码/来源和负责人摘要同组、右侧实体操作按钮；删除按钮使用危险色；概览改为左宽右窄，基础信息卡使用两列字段网格且 label/value 不缩进。
- [x] 2026-08-13 返修 19 验证：补充详情头标题状态同行、实体操作按钮、危险按钮、基础信息两列网格测试；刷新 `space-detail-1440.png` 与 `computed-style.json`，记录 `crumbBorderWidth=0px`、`detailHeadMinHeight=172px`、`titleRowDisplay=flex`、`actionButtonBackground=rgb(14, 16, 35)`、`dangerButtonBackground=rgb(212, 116, 118)`、`overviewGridTemplateColumns=754.656px 377.344px`、`fieldGridTemplateColumns=324.328px 324.328px`。
- [x] 2026-08-13 返修 20：根据验收反馈，空间详情页视觉尺度按原型压缩：详情头取消过高 `min-height` 并恢复约 18px padding；空间标题使用原型 h2 尺度；状态、编码、负责人和基础信息字段回到 12-14px 基准；负责人头像约 26px；右侧操作按钮恢复紧凑尺寸；详情 Tab、notice、卡片、配额行和时间线按原型密度收敛。
- [x] 2026-08-13 返修 20 验证：刷新 `space-detail-1440.png` 与 `computed-style.json`，记录 `detailHeadHeight=140px`、`detailHeadPaddingTop=18px`、`titleFontSize=24px`、`ownerAvatarWidth=26px`、`ownerAvatarHeight=26px`、`actionButtonHeight=36px`、`actionButtonPaddingTop=8px`、`actionButtonPaddingRight=13px`、`actionButtonBorderRadius=7px`、`baseCardPaddingTop=16px`、`fieldLabelFontSize=12px`、`fieldValueFontSize=14px`。
- [x] 2026-08-13 返修 21：根据验收反馈，确认空间详情页与空间列表页复用同一个后台 `AdminSidebar`，补充列表态/详情态侧边栏 DOM、active、折叠和用户菜单一致性证据；空间 API 返回负责人头像 URL，详情负责人头像复用后台统一 `AuthenticatedAvatar`，无头像时 fallback 首字；删除负责人姓名与“变更”之间的“负责人”文案；降低详情页面包屑、负责人、按钮、notice 和字段值等过度加粗。
- [x] 2026-08-13 返修 21 验证：前端回归、后端空间 API 集成测试和 1440px 证据通过；`computed-style.json` 记录 `sidebarReusedWithList=true`、列表/详情用户菜单项一致、`ownerAvatarHasImage=true`、`ownerTextContent=空间负责人变更`、除空间标题外关键文本字重均为 `400`。
- [x] 2026-08-13 返修 22：根据验收反馈，修复空间详情页内容过长时侧边栏被主内容撑高的问题；统一后台 `AdminSidebar` 改为视口高度稳定布局，底部用户触发区贴近视口底部，不随详情内容高度下沉。
- [x] 2026-08-13 返修 22 验证：补充详情页仍保留 `.admin-sidebar-user` 与 `.admin-user-trigger` 的回归断言；刷新 1440px 列表/详情视觉证据，`computed-style.json` 记录列表页与详情页 `sidebarUserBottomGap`、`userTriggerBottomGap` 一致且用户触发区可见。
- [x] 2026-08-13 返修 23：根据验收反馈，补齐空间详情页操作闭环：详情页变更、续期、冻结、删除、编辑和调整配额操作成功后直接同步当前详情数据；删除进入回收站后退出详情回到空间列表；操作弹窗标题按具体动作展示；移除未实现的成员“添加成员”入口，避免误导。
- [x] 2026-08-13 返修 23 验证：补充详情页入口交互回归测试，覆盖编辑、调整配额、续期、移交负责人、冻结和删除的弹窗标题、提交 API、成功 toast、详情态字段刷新与删除后跳转；刷新 1440px 证据，`computed-style.json` 记录 `freezeModalTitle=冻结空间`、`hasMemberAddButton=false`。
- [x] 2026-08-13 返修 24：根据验收反馈，核查空间详情成员、产品、配额与用量、操作记录分区；在不新增成员/审计 API 边界的前提下，成员与操作记录从伪真实静态行改为明确空态/数据骨架说明，产品分区保持真实绑定字段且不提供未实现操作，配额与用量分区保留可用调整配额入口。
- [x] 2026-08-13 返修 24 验证：补充分区切换、数据来源、空态/占位、产品无未实现操作和配额分区调整入口回归测试；刷新 1440px 证据，`computed-style.json` 记录成员无添加入口、无“成员占位”、产品无解绑/迁移/新增产品按钮、配额分区存在“调整配额”、操作记录无静态时间线。
- [x] 2026-08-13 返修 25：根据验收反馈，空间详情页“返回空间列表”改为无边框文字链接并补充 hover/focus 状态；成员分区删除“空间成员（同时为产品成员）”标题；成员内容复用概览页 `admin-space-detail-field-grid` 字段网格，统一 label/value 字号、字重和间距。
- [x] 2026-08-13 返修 25 验证：补充成员标题移除和字段网格复用测试；刷新 1440px 证据，`computed-style.json` 记录 `returnButtonBorderWidth=0px`、成员 `hasSectionTitle=false`、`fieldGridClassName=admin-space-detail-field-grid`、字段 label/value 字号 `12px/14px` 且字重均为 `400`。
- [x] 2026-08-13 返修 26：根据验收反馈，空间详情页成员 Tab 改为真实成员管理表格，字段为用户、角色、加入时间、状态、操作；负责人不展示在成员列表中，负责人变更仍走详情头流程；新增成员 API 闭环，支持添加状态正常用户、编辑管理员/编辑者/查看者角色和移除成员，并按角色和加入时间排序。
- [x] 2026-08-13 返修 26 验证：补充后端成员 API 集成测试、前端成员表格和添加/编辑/移除交互测试；刷新 1440px 证据，`computed-style.json` 记录成员表头 `[用户, 角色, 加入时间, 状态, 操作]`、`rowCount=2`、`hasOwnerInRows=false`、`firstRoleText=管理员`、表头/单元格字号 `12px/14px` 且字重均为 `400`。
- [x] 2026-08-13 返修 27：根据验收反馈，空间列表成员数、产品数和 AI 用量列按内容收敛但保留两行进度条表达；空间详情概览 Tab 移除产品绑定提示并移动到产品 Tab 顶部；成员列表用户头像复用后台统一头像组件；添加成员和编辑成员弹窗改为纵向字段布局；移除成员弹窗操作原因必填标签保持单行紧凑显示。
- [x] 2026-08-13 返修 27 验证：补充成员弹窗纵向布局、成员头像和原因标签 class 回归断言；刷新 1440px 视觉证据，`computed-style.json` 记录成员数列 `76px`、产品数列 `68px`、AI 用量列 `172px`、`overviewHasProductNotice=false`、产品 Tab 提示文案、`memberAvatarHasImage=true`、成员弹窗 `hasStackLayout=true`、原因标签 `display=inline-flex` 且 `white-space=nowrap`。
- [x] 2026-08-13 返修 28：根据验收反馈，修复添加成员和编辑成员弹窗中用户/角色下拉展开列表被弹窗滚动容器裁剪或遮罩的问题；成员弹窗内 `AdminSelect` 使用 body 级 fixed portal 菜单并携带后台主题变量，菜单宽度与触发器一致、背景不透明、层级高于弹窗内容。
- [x] 2026-08-13 返修 28 验证：补充成员弹窗用户/角色下拉 portal 回归断言；刷新 1440px 下拉展开视觉证据，`computed-style.json` 记录用户下拉、添加成员角色下拉和编辑成员角色下拉均 `parentIsBody=true`、`insideModal=false`、`position=fixed`、`backgroundColor=rgb(18, 20, 43)`、`widthMatchesTrigger=true`、`verticalGapFromTrigger=4`。
- [x] 2026-08-13 返修 29：根据验收反馈，成员 Tab 空态文案由“暂无普通成员；负责人不在成员列表中展示，请通过详情头变更负责人。”精简为“暂无成员，负责人不在成员列表。”。
- [x] 2026-08-13 返修 29 验证：补充成员空态文案回归测试；空间管理前端测试 9 个用例通过。
- [x] 2026-08-13 返修 30：根据验收反馈，修复空间详情成员头像旧路由 404；空间读模型复用头像 URL 规范化逻辑，将历史 `/api/v1/admin/users/avatar/{filename}` 统一转换为 `/api/v1/auth/avatar/{filename}`，覆盖空间详情负责人头像、成员列表，以及成员添加、编辑、移除后返回对象，不恢复旧头像读取接口。
- [x] 2026-08-13 返修 30 验证：补充后端空间成员 API 集成断言和前端头像路由回归测试，确认旧头像 URL 会被规范化，详情负责人和成员头像只请求 `/api/v1/auth/avatar/*`，不再请求 `/api/v1/admin/users/avatar/*`。
- [x] 2026-08-13 返修 31：根据验收反馈，空间详情产品 Tab 按原型和未来扩展性调整：顶部 notice 文案改为“绑定关系不可解除或迁移，产品名称始终与空间名称一致。”；下方改为产品卡片列表，一张 Card 对应一个产品，当前仅展示一个产品卡片；产品 Card 移除“绑定产品”标题和产品数字段，字段改为产品名称、产品 ID、绑定状态、研发状态，并复用详情统一字段网格。
- [x] 2026-08-13 返修 31 验证：补充产品 Tab 回归测试和 1440px 视觉证据，确认 notice 位于产品卡片上方、产品卡片数量为 1、无“绑定产品”标题、无产品数字段，字段网格/字号/字重/间距对齐详情页。
- [x] 2026-08-14 返修 32：根据验收反馈，空间详情操作记录 Tab 接入真实 `/api/v1/admin/spaces/{space_id}/audit-events`，展示操作、操作人、时间、来源、对象、变更前后值、原因和结果；审批通过创建空间时在新空间审计下补充审批来源追溯事件；确认编辑空间、添加成员和编辑成员角色不按高风险操作强制填写原因但仍写入审计。
- [x] 2026-08-14 返修 32 验证：补充后端审计集成测试、前端操作记录展示回归测试和 1440px 视觉证据，确认操作记录不再展示真实审计未接入占位，审批来源、成员操作审计、变更前后值、原因和结果可见。
- [x] 2026-08-14 返修 33：根据验收反馈，详情页编辑、调整配额、变更负责人、续期、冻结等操作成功后统一重新拉取当前空间详情和审计事件；概览最近操作改为基于真实 audit events 最新记录展示；操作记录 Tab 移除内部标题，操作/来源/结果/状态枚举改为轻量标签和中文文案，变更前后值改为关键字段变更摘要；后端审计读模型返回操作人展示名。
- [x] 2026-08-14 返修 33 验证：补充后端 `actor_display_name` 集成断言、前端详情刷新和操作记录表格回归测试；刷新 1440px 视觉证据和 computed style，确认最近操作随审计刷新、操作记录无内层标题、标签化展示且变更摘要不挤压列表。
- [x] 2026-08-14 返修 34：根据验收反馈，空间有效期固定日期改为支持时分秒输入/选择，默认值为当前季度最后一天 23:59:59，并在前后端校验固定日期必须晚于当前时间；长期有效不保存到期时间。概览基础信息字段顺序调整为空间名称、唯一标识、创建来源、创建时间、有效期，概览最近操作改为真实审计最新 6 条。操作记录 Tab 接入分页响应，列顺序调整为时间、操作人、操作动作、变更摘要、结果、操作，结果和操作动作标签化，行内查看打开右侧抽屉展示来源、对象、request_id、完整变更前后值、原因和结果。
- [x] 2026-08-14 返修 34 验证：补充后端固定日期过期校验和审计分页集成断言、前端时间控件秒级默认值/最小值、最近 6 条、操作记录分页、标签化表格和详情抽屉回归测试；刷新 1440px 视觉证据和 computed style，确认审计分页、抽屉和详情概览字段顺序可见。
- [x] 2026-08-14 返修 35：根据验收反馈，空间详情操作记录 Tab 分页改为轻量无边框样式，保留 hover、focus-visible 和 active 状态；操作详情抽屉中变更前/变更后按 JSON 格式化 code block 展示，解析失败时降级显示原始文本，长内容在 code 区域内滚动；空间列表长期有效空间的有效期文案显示为“长期有效”。
- [x] 2026-08-14 返修 35 验证：补充空间列表长期有效文案、审计抽屉 JSON 格式化展示回归测试；刷新 1440px 视觉证据和 computed style，确认操作记录分页边框为 0、hover/active 状态可见、JSON 区域等宽字体且内部滚动。
- [x] 2026-08-14 返修 36：根据验收反馈，空间详情成员 Tab 表格在不放大字号的前提下增加行高和单元格垂直间距，头像、用户信息和操作保持垂直居中；概览 Tab 最近操作中的操作动作取消过度加粗，仅保留卡片标题强调。
- [x] 2026-08-14 返修 36 验证：刷新 1440px 视觉证据和 computed style，确认成员表格行高约 52px、单元格上下 padding 为 8px、垂直居中，最近操作动作字重为 400；前端空间管理和用户管理回归测试通过。
- [x] 2026-08-14 返修 37：根据验收反馈，空间有效期到期时间从原生 `datetime-local` 替换为自研轻量 `AdminDateTimePicker`，输入框展示 `yyyy-mm-dd hh:mm:ss`，展开面板同时支持日期和时分秒选择，保留手动输入与失焦格式化；固定日期默认当前季度最后一天 23:59:59 且仍按既有规则校验必须晚于当前时间，长期有效隐藏到期时间。
- [x] 2026-08-14 返修 37 验证：补充创建空间日期时间控件交互回归测试；刷新 1440px 创建空间弹窗日期时间控件截图和 computed style，确认面板为 body 级 fixed 浮层、背景不透明、位于输入框下方且不覆盖触发器，包含日历、时/分/秒输入和快捷项。
- [x] 2026-08-14 返修 38：根据验收反馈，操作记录详情抽屉中变更前/变更后为空、null、空字符串或“无”时，不再使用大块 JSON code block，改为轻量空态文本“无”；仅非空 JSON 或原始文本继续使用 code block，长内容仍在 code 区域内滚动。
- [x] 2026-08-14 返修 38 验证：补充操作记录抽屉 JSON 值和空值回归测试；刷新 1440px 空值抽屉视觉证据和 computed style，确认空态不撑高抽屉，变更前/后统一处理。
- [x] 2026-08-14 返修 39：根据验收反馈，以空间详情成员 Tab 的“添加成员”按钮为起点，统一调整管理后台金色主按钮在亮主题下的文字颜色和字重；亮主题主按钮文字改为主题适配深棕文本，字重降为中等，hover/focus-visible 保持可见反馈；暗主题保持可读不回归。
- [x] 2026-08-14 返修 39 验证：补充 1440px 亮主题成员 Tab 视觉证据和 computed style，确认亮主题主按钮 `color=rgb(62, 43, 18)`、`font-weight=500`、focus outline 可见；暗主题主按钮 `color=rgb(9, 11, 22)`、`font-weight=500` 保持可读。
- [x] 2026-08-14 返修 40：根据验收反馈，空间回收站 Tab 改为独立请求 `status=RECYCLE` 真实分页数据，不再从默认空间列表本地过滤；回收站分页 total 使用回收站接口返回值；回收站表格补齐删除时间、删除人、删除原因、剩余天数和查看/恢复/彻底删除操作；删除空间后刷新列表与回收站，确保可在回收站看到已删除空间。
- [x] 2026-08-14 返修 40 验证：补充后端回收站读模型字段集成断言、前端回收站独立接口/分页/删除后跳转回收站回归测试；刷新 `space-recycle-1440.png` 与 `computed-style.json`，确认回收站标题、表头、删除字段、分页 total 和操作入口可见。
- [x] 2026-08-14 返修 41：根据验收反馈，修复回收站 Tab 行内“更多”菜单被遮罩的问题；回收站更多菜单复用普通空间列表 body 级 fixed 浮层能力，菜单锚定当前回收站行“更多”按钮下方、左对齐、保留 6px 间隙，层级高于表格、分页、sticky 操作列和回收站容器，并继续适配亮/暗主题背景与边框。
- [x] 2026-08-14 返修 41 验证：补充回收站更多菜单前端回归测试和 1440px 视觉证据；`computed-style.json` 记录 `moreMenuScope=recycle`、`moreMenuParentIsBody=true`、`moreMenuPosition=fixed`、`moreMenuZIndex=1100`、`moreMenuLeftAlignedToTrigger=true`、`moreMenuVerticalGapFromTrigger=6`、`moreMenuDoesNotCoverPagination=true`。
- [x] 2026-08-14 返修 42：根据验收反馈，继续修复回收站 Tab 更多菜单真实页面只露出边框/底边、菜单项文字不可见的问题；更多菜单定位逻辑改为根据菜单高度、分页位置和视口底部空间判断展开方向，靠近分页或视口底部时向上展开，确保菜单面板和菜单项文字完整可见。
- [x] 2026-08-14 返修 42 验证：补充回收站分页贴近按钮场景的前端回归测试；刷新 `space-recycle-more-1440.png` 与 `computed-style.json`，确认 `moreMenuPlacement=top`、`moreMenuHeight=34`、`moreMenuItemTexts=[彻删]`、`moreMenuFirstItemVisible=true`、`moreMenuDoesNotCoverPagination=true`。
- [x] 2026-08-14 返修 43：根据验收反馈，回收站更多菜单上一轮向上展开属于过度修正；菜单位置回退为始终优先显示在当前行“更多”按钮正下方、左对齐并保留 6px 间隙，不得向上漂移。真正修复点改为避免无更多操作时渲染空面板：仅当存在配额、续期、负责人、删除或彻删等收敛操作时展示“更多”按钮，回收站无彻删权限时不再出现空边框面板。
- [x] 2026-08-14 返修 43 验证：补充“无更多操作不渲染更多按钮/空面板”前端回归测试；刷新 `space-recycle-more-1440.png` 与 `computed-style.json`，确认 `moreMenuPlacement=bottom`、`moreMenuVerticalGapFromTrigger=6`、`moreMenuItemTexts=[彻删]`、`moreMenuFirstItemOnTop=true`、`moreMenuZIndexAbovePagination=true`。
- [x] 2026-08-14 返修 44：根据验收反馈，前台创建空间申请入口暂未实现时，补充开发/演示环境后端待审批申请 seed 能力；新增 `ADMIN_SPACE_APPLICATION_DEMO_SEED` 开关和 `scripts/seed-admin-space-applications.py` 手动脚本，演示数据走真实空间申请创建逻辑，生产环境不自动播种，前端不硬编码生产 Mock。
- [x] 2026-08-14 返修 44 验证：补充后端集成测试，确认 seed 生成 3 条待审批申请、重复 seed 幂等、审批通过后创建来源为“申请审批”的空间、待审批列表减少、审计记录包含 `application_approved_create_space`；同步部署环境变量说明、Change 设计与 spec delta。
- [x] 2026-08-14 返修 45：根据验收反馈，修复手动 seed 脚本默认写入 `data/sqlite/moonbox.db` 导致 Docker 后台申请审批 Tab 无数据的问题；脚本改为加载 `.env`，将容器内 SQLite 路径 `/app/data/sqlite/moonbox.db` 映射为宿主机 `data/runtime/backend/sqlite/moonbox.db`，并输出目标数据库路径和播种数量。
- [x] 2026-08-14 返修 45 验证：补充脚本目标库解析单元测试，覆盖 Docker SQLite 路径映射和显式宿主机 SQLite URL 不被 `.env` 覆盖；直接运行脚本确认输出目标运行库并生成 3 条待审批申请，运行库查询可见 `ai-factory-demo`、`delivery-space-demo`、`growth-lab-demo`。
- [x] 2026-08-14 返修 46：根据验收反馈，申请审批 Tab 从横向大卡片改为后台统一紧凑审批表格，字段为空间名称/编码、申请人、负责人、资源申请、有效期、申请时间和操作；空间名称/编码两行展示，资源申请格式化展示成员/存储/AI Tokens，操作改为用户管理同款无边框文字操作。
- [x] 2026-08-14 返修 46 验证：补充审批表格结构、字段格式、分页 DOM 和操作样式前端回归测试；刷新 1440px 申请审批视觉证据和 computed style，确认不再使用审批卡片布局，审批表格、colgroup、分页和轻量操作可见；本轮仅补真实分页，不新增搜索/筛选，避免未确认后端查询交互时做半功能。
- [x] 2026-08-14 返修 47：根据验收反馈，修复申请审批表格资源申请列分隔符显示为 `Â·` 且与有效期列重叠的问题；资源申请改为成员、存储、AI Tokens 三个独立紧凑资源项，不再使用分隔符伪元素；AI Tokens 改为中文短量级格式，如 `90万 Tokens`、`150万 Tokens`。
- [x] 2026-08-14 返修 47 验证：补充前端回归测试断言资源项数量、短量级 Tokens 和无 `Â·`；刷新 1440px 申请审批视觉证据和 computed style，记录资源列与有效期列不重叠。
