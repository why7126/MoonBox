## 1. UI Skeleton 与合同落地

- [x] 1.1 在需求中心用户菜单空间切换区域建立 UI Skeleton，覆盖 SpacePopover、SpaceList、SpaceOption、LoadingState、EmptyState、ErrorState 和 CreateOrJoinEntry。
- [x] 1.2 为空间浮层、空间项、当前项、冻结项、空态和错误态补齐稳定选择器或等价测试定位。
- [x] 1.3 在 1440px 桌面视口完成 Skeleton 首轮截图或等价视觉证据，确认一级菜单、二级浮层、空间项密度和层级方向正确。

## 2. 后端真实空间上下文

- [x] 2.1 在需求中心 BFF 或前台空间上下文服务中接入后台空间、成员和产品绑定事实源。
- [x] 2.2 实现当前用户已加入空间过滤，排除未加入、无权限和回收中空间。
- [x] 2.3 为冻结空间返回只读或冻结语义，并保留服务端状态校验边界。
- [x] 2.4 对前台空间响应执行字段白名单映射和错误脱敏，避免暴露后台配额、审计、删除原因、内部 ID、高风险动作和敏感错误。

## 3. 前端空间切换接入

- [x] 3.1 将空间浮层运行时数据切换为真实上下文接口返回的 `workspaces`，移除生产路径静态空间 fallback。
- [x] 3.2 实现本地最近选择校验、失效清理和默认空间回退。
- [x] 3.3 实现冻结空间只读标记、无已加入空间空态、接口失败错误态和加载态。
- [x] 3.4 切换空间后同步用户区空间名称、当前项、最近选择、需求中心上下文、统计、筛选和看板刷新。

## 4. 测试与验收

- [x] 4.1 补齐后端测试：已加入空间过滤、未加入不可见、回收隐藏、冻结只读、普通前台用户不能读取后台全量空间。
- [x] 4.2 补齐前端测试：真实空间列表渲染、当前项勾选、冻结标记、最近选择失效回退、无空间空态、接口失败和切换后上下文刷新。
- [x] 4.3 完成 1440px 视觉验收，覆盖用户菜单、空间浮层、正常/冻结/空态/错误态和 click outside/Escape 关闭。
- [x] 4.4 记录 computed style 或等价检查，覆盖浮层层级、宽度、overflow、空间行间距、冻结标记颜色和深浅主题可读性。

## 5. 文档与同步

- [x] 5.1 如新增或调整 API 字段，同步 API 文档、OpenAPI 来源和客户端调用约束。
- [x] 5.2 如涉及空间权限或冻结状态边界，同步安全相关测试或规则说明。
- [x] 5.3 在 Change trace 回填 UI Skeleton、视觉证据、computed style、Mock/API 边界和 REQ 最终一致性状态。
