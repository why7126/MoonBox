---
change_id: add-space-creation-join-application-flow
status: modified
created_at: 2026-08-15 11:08:25
updated_at: 2026-08-16 10:15:00
---

# Tasks

## 1. 设计与 Skeleton

- [x] 1.1 按附件 v1.0.1 确认 REQ、prototype、acceptance、UI Contract 和 Mock/API 边界一致。
- [x] 1.2 实现创建空间弹窗 UI Skeleton，覆盖标题、审批说明、基础信息、配额、有效期、到期时间、底部操作和结果态。
- [x] 1.3 在 1440px 桌面视口对创建空间弹窗做返修视觉验收。

## 2. 后端与数据

- [x] 2.1 调整前台创建空间申请请求模型，覆盖标识 2-32、成员 1-100000、存储大于 0GB、AI Tokens 不小于 0 和后台一致到期时间。
- [x] 2.2 实现前台提交待审批空间申请，平台管理员审批通过后创建正式空间。
- [x] 2.3 移除前台加入空间、精准搜索、我的申请、撤回和重新提交路由暴露。
- [x] 2.4 复用后台空间申请审批作为正式空间创建事实源。

## 3. 前端

- [x] 3.1 将空间切换入口文案改为“创建空间”。
- [x] 3.2 移除加入空间 Tab、精准搜索、我的申请列表、撤回和重提入口。
- [x] 3.3 实现创建空间表单、必填红色星号、名称自动生成标识、手动标识锁定、后台一致配额限制和到期时间控件视口限位。
- [x] 3.4 实现提交中“正在创建...”、待审批结果态和无进入空间入口。

## 4. 测试与验收

- [x] 4.1 补充后端 pytest，覆盖提交待审批申请、申请人/拟负责人、配额字段、join 路由不可用和越界拒绝。
- [x] 4.2 补充前端 Vitest/Testing Library，覆盖创建入口、无加入入口、自动标识、提交和待审批态。
- [x] 4.3 完成 1440px 视觉验收和 computed style 证据刷新。
- [x] 4.4 回填 Change trace、REQ acceptance 和 Mock/API 边界。

## 5. 文档同步

- [x] 5.1 同步 `docs/03-api-index.md` 前台创建空间 API。
- [x] 5.2 同步 `docs/04-database-design.md` DB 与后台申请兼容边界。
- [x] 5.3 明确产品手册在发布阶段按“创建空间”口径同步。

## 验收返修记录

| 时间 | 反馈 | 调整 |
|---|---|---|
| 2026-08-15 12:27:30 | 用户选择 A：按附件 v1.0.1 更新，删除加入空间能力，入口只用于创建空间。 | 前台收敛为创建空间弹窗；后端前台创建接口直接创建空间；OpenSpec/REQ/docs/tests/视觉证据同步更新。 |
| 2026-08-15 12:52:00 | 用户验收指出附件内容没有完整更新到 REQ 目录。 | 补齐 REQ 子文档 `business-flow.md`、`user-stories.md` 和 `prototype/web/prototype.html`，全部收敛为仅创建空间。 |
| 2026-08-15 13:30:00 | 用户验收指出实现效果与附件原型 UI 不一致，原型列布局更好看；负责人卡不需要邮箱。 | 优化创建空间弹窗：复刻双列网格、输入框右侧单位、负责人卡单行只读提示、AI Tokens/有效期同一行、右下角取消/创建按钮，并刷新 1440px 视觉证据。 |
| 2026-08-15 14:31:00 | 用户要求说明需平台管理员审批后才可使用、必填红星、删除负责人卡、到期日期放 AI Tokens 下方、单位与后台空间管理一致。 | 前台改为提交待审批申请；删除负责人卡；必填项加红星；AI Tokens 改为 token 数值；成功态改为待审批；REQ/OpenSpec/API 文档同步。 |
| 2026-08-15 14:52:00 | 用户要求创建空间弹窗输入限制和校验与后台空间管理一致，AI Tokens 不显示 `Tokens` 单位，到期日期改为到期时间并默认本季度最后一天 23:59:59。 | 前台/后端申请 schema 对齐后台成员、存储、AI Tokens 边界；AI Tokens 移除右侧单位；到期时间默认本季度末并复用后台日期时间选择器；测试与文档同步。 |
| 2026-08-15 15:03:00 | 用户反馈创建空间弹窗到期时间选择器超出 1440x900 可视区域，无法完成选择确认。 | 到期时间浮层增加视口碰撞处理：下方空间不足时向上展开并保持在视口内；补充前端测试、视觉证据和验收文档。 |
| 2026-08-15 15:12:00 | 用户反馈到期时间控件不会消失、会出现滚动条，弹窗右上角关闭按钮看不清。 | 到期时间选择器增加“取消 / 确定”操作，点击确定后收起；面板取消自身滚动条；弹窗关闭按钮增强尺寸、边框、背景和 hover/focus 状态。 |
| 2026-08-15 15:20:00 | 用户反馈时间控件使用“取消 / 确定”偏重，并确认 2 种退出方式：点击 3 个快捷按钮、点击时间控件外部区域。 | 移除时间面板内“取消 / 确定”；快捷按钮立即应用并关闭；点击外部区域关闭并保留当前值；继续保持无面板滚动条和右上角关闭按钮清晰。 |
| 2026-08-15 16:25:00 | 用户验收指出点击到期时间控件外部区域后，控制面板没有消失。 | 将时间控件外部点击监听改为 document capture 阶段，避免被创建空间弹窗内部 `stopPropagation` 拦截；补充弹窗内非控件区域点击关闭且保留当前值、日历图标二次点击关闭的测试。 |
| 2026-08-16 10:15:00 | 用户建议将弹窗内审批提示整合到弹窗副标题，减少重复文案。 | 副标题整合空间隔离、提交审批、审批通过后创建空间并分配负责人的说明；删除表单上方重复提示行；同步测试、prototype、REQ 和 OpenSpec 文档。 |
